import Coach from "../components/Coach";

export const metadata = {
  title: "Protocolo — Coach",
  description: "Pon a prueba tus conocimientos de protocolo en una conversación con un experto",
  alternates: { canonical: "https://protocolo.turel.es/coach" },
};

export default function CoachPage() {
  return <Coach />;
}
